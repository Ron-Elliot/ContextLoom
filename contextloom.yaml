project_id: contextloom
name: ContextLoom
sources:
  - type: git-repo
    path: .
    globs:
      - '**/*.ts'
      - '**/*.md'
      - '**/*.yaml'
      - '**/*.json'
    extractors:
      - default
cross_project_links: []
serve:
  trust_filter:
    allow_review_statuses: [candidate, reviewed]
